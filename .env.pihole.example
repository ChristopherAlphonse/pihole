# Pi-hole v6 Configuration Example
# Copy this file to .env.pihole and customize as needed

# Timezone (find yours at https://en.wikipedia.org/wiki/List_of_tz_database_time_zones)
TZ=America/New_York

# Leave empty for no password (recommended for local use)
# Or set a password hash generated by: echo -n "yourpassword" | sha256sum
FTLCONF_webserver_api_password=

# DNS listening mode: "all" to accept queries from any source
# Options: local, all, single, bind
FTLCONF_dns_listeningMode=all
